// TODO: unmatched_questions review — add a listing here to browse answer-bank
// misses captured during real runs and turn them into new apply.ts rules.
// Deferred until after the backend has been stress-tested with real runs.
export function Home(): React.JSX.Element {
  return (
    <div className="home">
      <h1 className="home-title">Home</h1>
      <p className="home-empty">Dashboard content coming soon.</p>
    </div>
  )
}
