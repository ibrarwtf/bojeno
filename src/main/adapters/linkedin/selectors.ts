export const linkedinSelectors = {
  /** Navigating here while logged out redirects to a /login URL. */
  loginCheckUrl: 'https://www.linkedin.com/feed/',
  /** Present in the DOM only when a session is authenticated. */
  loggedInIndicator: '#global-nav'
}
